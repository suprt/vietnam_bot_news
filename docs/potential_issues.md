# Потенциальные проблемы и улучшения

Этот документ содержит список потенциальных проблем и улучшений, которые можно реализовать в будущем, но не критичны для MVP.

## 🔵 Не критичные, но полезные улучшения

### 1. Экранирование Markdown-символов

**Проблема:** Если в заголовке или summary новости есть Markdown-символы (`*`, `_`, `[`, `]`, `` ` ``), они могут сломать форматирование сообщения в Telegram.

**Решение:** Экранировать специальные символы в `entry.Title` и `entry.SummaryRU` перед форматированием.

**Статус:** Отложено до необходимости.

### 2. Ограничение длины контента для Gemini

**Текущая реализация:** Используется только контент из RSS (description или content:encoded), который обычно краткий (100-500 символов). Мы **не парсим** полный текст статьи со страницы по ссылке.

**Ответ:** В RSS лентах действительно только описание/аннотация, не полный текст. Мы используем только то, что есть в RSS. Полный текст статьи со страницы не достаём.

**Потенциальная проблема:** Если в будущем добавим парсинг полных статей, очень длинные тексты могут превысить лимиты Gemini API.

**Решение (на будущее):** Если понадобится, добавить ограничение длины контента перед отправкой в Gemini (например, первые 5000 символов).

### 3. Дубликаты ID статей

**Проблема:** Если одна новость попадает в несколько RSS-лент одного сайта, возможны дубликаты с разными ID (из-за timestamp в ID).

**Текущее решение:** Дедупликация через `canonicalKey` в фильтре (по URL/заголовку).

**Статус:** Работает, но можно улучшить формирование ID для более точной дедупликации.

### 4. Ограничение длины summary

**Проблема:** Gemini может вернуть очень длинный summary, который сломает форматирование или превысит лимиты Telegram.

**Текущее решение:** Нет ограничения, но summary должен быть 1-2 предложения по промпту.

**Потенциальное улучшение:** Добавить валидацию и обрезку слишком длинных summary.

### 5. Ограничение количества новостей на входе

**Проблема:** Если RSS-ленты вернут тысячи статей, весь пайплайн может быть очень медленным и дорогим (Gemini API).

**Текущее решение:** Фильтрация по свежести и длине контента.

**Потенциальное улучшение:** Добавить общий лимит на количество статей после фильтрации (например, топ-200 самых свежих).

### 6. Улучшение обработки ошибок Gemini API

**Текущее решение:** Fallback на дефолтные значения при ошибках.

**Потенциальное улучшение:** 
- Более гранулярная обработка ошибок (rate limits, quota exceeded, etc.)
- Exponential backoff для rate limits
- Логирование детальной информации об ошибках

### 7. Валидация конфигурации

**Проблема:** Некорректные значения в конфиге (отрицательные, нули) могут вызвать неожиданное поведение.

**Решение:** Добавить валидацию при загрузке конфига с понятными сообщениями об ошибках.

**Статус:** Планируется для улучшения.

### 8. Лимит на количество статей из RSS

**Проблема:** RSS может содержать тысячи статей за все время, но нам нужны только свежие.

**Текущее решение:** 
- Фильтрация по `recency_max_hours` в фильтре
- Лимит на первые 100 статей из одного RSS feed (они обычно самые свежие)

**Статус:** ✅ Реализовано.

### 9. Статьи с датой в будущем

**Проблема:** Некорректные даты в RSS могут привести к статьям с `PublishedAt` в будущем.

**Решение:** Фильтровать такие статьи в фильтре.

**Статус:** ✅ Реализовано.

### 10. Race condition при отправке приветствия

**Проблема:** Если несколько updates от одного пользователя обрабатываются одновременно, может отправиться несколько приветствий.

**Текущее решение:** Goroutine без синхронизации.

**Статус:** Низкий приоритет, маловероятно в реальности.

