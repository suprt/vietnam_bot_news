# Настройка первого запуска

Пошаговая инструкция для подготовки и запуска системы в первый раз.

## 📋 Чеклист подготовки

### Шаг 1: Получение API ключей

#### 1.1 Telegram Bot Token

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например, "Vietnam News Digest")
   - Введите username бота (должен заканчиваться на `bot`, например `vietnam_news_bot`)
4. BotFather вернёт токен вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
5. **Сохраните токен** — он понадобится на следующем шаге

#### 1.2 Gemini API Key

1. Перейдите на https://ai.google.dev/
2. Войдите в свой Google аккаунт
3. Перейдите в раздел "Get API key" или "API Keys"
4. Создайте новый API ключ (или используйте существующий)
5. **Сохраните ключ** — он понадобится на следующем шаге

**Важно:** Убедитесь, что у вас есть доступ к бесплатному плану Gemini Flash:
- RPM (Requests Per Minute): 10 запросов в минуту
- TPM (Tokens Per Minute): 250,000 токенов в минуту
- RPD (Requests Per Day): 250 запросов в день

### Шаг 2: Настройка секретов в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте два секрета:

   **Секрет 1:**
   - Name: `GEMINI_API_KEY`
   - Secret: вставьте ваш Gemini API ключ
   - Нажмите **Add secret**

   **Секрет 2:**
   - Name: `TELEGRAM_BOT_TOKEN`
   - Secret: вставьте ваш Telegram Bot Token
   - Нажмите **Add secret**

5. Убедитесь, что оба секрета созданы и видны в списке

### Шаг 3: Проверка конфигурации

Проверьте, что файлы конфигурации на месте:

- ✅ `configs/pipeline.yaml` — параметры пайплайна
- ✅ `configs/sites.yaml` — список RSS-лент (уже оптимизирован)

Если нужно изменить параметры (например, количество статей на категорию), отредактируйте `configs/pipeline.yaml`.

### Шаг 4: Первый тестовый запуск

#### Вариант A: Запуск через GitHub Actions (рекомендуется)

1. Перейдите в раздел **Actions** вашего репозитория
2. В левом меню выберите workflow **Daily News Digest**
3. Нажмите **Run workflow** (справа вверху)
4. В выпадающем меню:
   - Выберите ветку (обычно `main` или `master`)
   - В поле **force_dispatch** введите `1` (для принудительной рассылки)
5. Нажмите **Run workflow**

6. Дождитесь завершения выполнения:
   - Нажмите на запущенный workflow
   - Следите за логами выполнения
   - Проверьте, что все шаги завершились успешно (зелёные галочки)

#### Вариант B: Локальный запуск (для отладки)

Если хотите протестировать локально перед запуском в GitHub Actions:

```bash
# Установите переменные окружения
export GEMINI_API_KEY="ваш-ключ-здесь"
export TELEGRAM_BOT_TOKEN="ваш-токен-здесь"
export FORCE_DISPATCH="1"

# Запустите приложение
go run ./cmd/dailyjob
```

**Примечание:** При локальном запуске `state/state.json` будет создан локально и не будет синхронизирован с GitHub.

### Шаг 5: Подписка первого пользователя

1. Найдите вашего Telegram бота по username (который вы указали при создании)
2. Откройте диалог с ботом
3. Отправьте команду `/start` или любое сообщение
4. Бот должен ответить приветственным сообщением (если настроено)

5. Запустите пайплайн снова (через GitHub Actions с `force_dispatch: 1`):
   - При запуске система автоматически обнаружит нового пользователя
   - `chat_id` будет сохранён в `state/state.json`
   - Если есть новости для отправки, они будут отправлены

6. Проверьте результат:
   - В логах GitHub Actions должно быть сообщение о найденных получателях
   - В Telegram должен прийти дайджест (если есть новости)

### Шаг 6: Проверка состояния

После успешного запуска проверьте:

1. **Файл состояния:**
   - В репозитории должен появиться/обновиться файл `state/state.json`
   - Проверьте, что в нём есть:
     - `recipients` — список подписчиков
     - `sent_articles` — список отправленных новостей
     - `telegram.last_update_id` — последний обработанный update

2. **Логи GitHub Actions:**
   - Проверьте, что нет ошибок
   - Должны быть логи о количестве собранных новостей
   - Должны быть логи о категоризации и отправке

## 🔍 Диагностика проблем

### Проблема: Workflow завершился с ошибкой

**Решение:**
1. Откройте логи выполнения workflow
2. Найдите шаг, на котором произошла ошибка
3. Проверьте сообщение об ошибке
4. Частые причины:
   - Неверный API ключ или токен
   - Недоступность RSS-лент
   - Ошибки парсинга конфигурации

### Проблема: Новости не отправляются

**Решение:**
1. Проверьте, что пользователь подписан (отправил `/start` боту)
2. Проверьте логи — должно быть сообщение о найденных получателях
3. Убедитесь, что есть новые новости (не все отфильтрованы)
4. Попробуйте запустить с `force_dispatch: 1`

### Проблема: State не коммитится

**Решение:**
1. Проверьте права доступа GitHub Actions к репозиторию
2. Убедитесь, что workflow имеет права на запись
3. Проверьте, что файл `state/state.json` действительно изменился

### Проблема: Gemini API ошибки

**Решение:**
1. Проверьте лимиты API (RPM, TPM, RPD)
2. Убедитесь, что API ключ активен
3. Проверьте логи на конкретные ошибки от Gemini API

## ✅ Готово к работе

После успешного первого запуска:

1. ✅ Система будет автоматически запускаться каждый день в 01:00 UTC (08:00 по Вьетнаму)
2. ✅ Новые пользователи могут подписаться, отправив `/start` боту
3. ✅ Дайджест будет формироваться и отправляться автоматически
4. ✅ Состояние будет сохраняться между запусками

## 📝 Дополнительные настройки

### Изменение времени запуска

Отредактируйте `.github/workflows/news_daily.yml`:
```yaml
schedule:
  - cron: '0 1 * * *'  # Измените на нужное время (UTC)
```

### Добавление новых RSS-лент

Отредактируйте `configs/sites.yaml` и добавьте новые фиды в соответствующий сайт.

### Изменение параметров фильтрации

Отредактируйте `configs/pipeline.yaml`:
- `recency_max_hours` — максимальный возраст новостей
- `min_content_length` — минимальная длина контента
- `max_articles_per_category` — максимум статей на категорию

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте логи GitHub Actions
2. Проверьте документацию в `docs/`
3. Убедитесь, что все секреты настроены правильно

